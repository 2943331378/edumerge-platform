# ===== 开发环境配置 (application-dev.yml) =====
# 启用此配置: spring.profiles.active=dev

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/edumerge_dev?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: 123456

  data:
    redis:
      host: localhost
      port: 6379
      password: ""

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

milvus:
  host: localhost
  port: 19530

langchain4j:
  ollama:
    url: http://localhost:11434
    model: llama2

logging:
  level:
    root: debug
    com.edumerge: debug
